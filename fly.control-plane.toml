app = "lag-dowsing-rod-control"
primary_region = "iad"

[build]
  dockerfile = "control-plane/Dockerfile"

[http_service]
  internal_port = 4000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
